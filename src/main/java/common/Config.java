package common;

public class Config {

    /**
     * Clean browser cookie after each iteration
     */
    public static final boolean IS_CLEAR_COOKIES = true;
    /**
     * To keep the browser open after all scenario/tests
     */
    public static final boolean IS_BROWSER_OPEN = true;
    /**
     * Clear the directory with the screens before starting the tests
     */
    public static final boolean CLEAN_REPORTS_DIR = true;
}
